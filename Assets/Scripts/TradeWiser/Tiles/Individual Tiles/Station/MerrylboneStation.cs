
public class MerrylboneStation : Station
{
    // Start is called before the first frame update
    void Start()
    {
        SetTileInfo(15, TypeOfTile.Station);
        SetStation("Merrylbone Station", 200);
    }
}
