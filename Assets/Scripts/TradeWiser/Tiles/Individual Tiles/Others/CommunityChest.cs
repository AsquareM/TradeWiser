using UnityEngine;

public class CommunityChest : Tile
{
    // Start is called before the first frame update
    void Start()
    {
        SetTileInfo(TileIndex, TypeOfTile.CommunityChest);
    }

}
