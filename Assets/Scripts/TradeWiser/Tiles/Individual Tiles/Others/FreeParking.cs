
public class FreeParking : Tile
{
    // Start is called before the first frame update
    void Start()
    {
        SetTileInfo(20, TypeOfTile.FreeParking);
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
